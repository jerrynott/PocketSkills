# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy ASP.Net Core app to Azure Web App - dbtcbttherapistportal

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up .NET Core
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '3.0'

    - name: Print working directory
      run: ls C:\"Program Files"\dotnet\sdk
    - run: C:\"Program Files"\dotnet\sdk\9.0.202\
    - run: C:\"Program Files"\dotnet\sdk\9.0.202\Microsoft\
    - run: C:\"Program Files"\dotnet\sdk\9.0.202\Microsoft\VisualStudio\
    - run: C:\"Program Files"\dotnet\sdk\9.0.202\Microsoft\VisualStudio\v17.0\
            
      
    - name: Build with dotnet
      run: dotnet build --configuration Release -v d

    - name: dotnet publish
      run: dotnet publish -c Release -o ${{env.DOTNET_ROOT}}/myapp

    - name: Deploy to Azure Web App
      uses: azure/webapps-deploy@v1
      with:
        app-name: 'dbtcbttherapistportal'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_95c88d7a4d9d4190ae88ceceb1f84541 }}
        package: ${{env.DOTNET_ROOT}}/myapp 
